name = "web-analyzer-pro"
pages_build_output_dir = ".vercel/output/static"

compatibility_date = "2025-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Add non-sensitive vars here if needed
# Sensitive vars (API keys) should be added in the Cloudflare Dashboard

[[workflows]]
binding = "SCAN_WORKFLOW_SERVICE"
name = "scan-workflow"
class_name = "ScanWorkflow"


